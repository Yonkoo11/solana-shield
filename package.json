{
  "name": "solana-shield",
  "version": "0.1.0",
  "description": "On-chain guardian protocol for Solana — detect exploits, pause protocols, protect TVL",
  "private": true,
  "scripts": {
    "test": "anchor test",
    "build": "anchor build"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/web3.js": "^1.98.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.3.0",
    "mocha": "^10.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
